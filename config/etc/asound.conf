# ALSA default audio device
# Routes all audio to the 3.5mm analog jack (bcm2835 Headphones, card 1)
# Card 0 = bcm2835 HDMI (digital out — not used)
# Card 1 = bcm2835 Headphones (3.5mm jack — default)
pcm.!default {
    type hw
    card 1
    device 0
}
ctl.!default {
    type hw
    card 1
}
